// 모달 전역에서 재사용하는 기본 상수 모음
export const ANIMATION_DURATION = 200;
export const FOCUSABLE_SELECTOR =
  'a[href], area[href], button:not([disabled]), input:not([disabled]), textarea:not([disabled]), select:not([disabled]), summary, [tabindex]:not([tabindex="-1"])';
